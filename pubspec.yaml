name: chika_metronome
description: A new Flutter application.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2

  bloc: ^3.0.0
  flutter_bloc: ^3.2.0
  equatable: ^1.1.0

  soundpool: ^1.0.1

  flutter_picker: ^1.1.0

  url_launcher: ^5.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/sounds/